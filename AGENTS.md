1. Le projet utilise `bun`. Ne pas utiliser `npm` mais `bun`.
2. Ne pas lancer `bun run dev` car il est déjà lancé sur le port TCP 5173
3. Le projet doit être formatté avec prettier et sa commande `bun format`
4. Les pages du site doivent être dans `/src/pages`
5. Le projet doit utiliser tailwind et le moins possible de CSS natif
6. Utiliser le conventional commit
